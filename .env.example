# MariaDB 자격증명 — docker compose가 이 디렉터리의 .env를 자동 로드해 치환한다.
# ⚠️ 여기 값을 바꾸면 api/.env 의 DATABASE_URL 도 동일하게 맞춰야 한다.
MARIADB_ROOT_PASSWORD=root
MARIADB_DATABASE=goods_mall
MARIADB_USER=goods
MARIADB_PASSWORD=goods
MARIADB_PORT=3306
