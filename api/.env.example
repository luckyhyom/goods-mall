# 애플리케이션
PORT=4000

# CORS — 허용할 웹 오리진
CORS_ORIGIN=http://localhost:3000

# 데이터베이스
DATABASE_URL="mysql://goods:goods@localhost:3306/goods_mall"

# 인증 (Slice 1) — 시크릿은 실제 값으로 교체할 것
JWT_ACCESS_SECRET=change-me-access-secret
JWT_ACCESS_TTL=15m
REFRESH_TTL_DAYS=3
